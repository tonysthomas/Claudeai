import { Bot } from 'lucide-react'
import Placeholder from './Placeholder'

export default function Agents() {
  return (
    <Placeholder
      icon={Bot}
      title="Agents"
      description="Monitor and control the Data, Analytics, Alert, and Report agents. View run logs, status, and trigger manual runs."
    />
  )
}
